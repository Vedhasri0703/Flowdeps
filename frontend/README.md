# FlowDeps — Frontend

This is the React + Vite frontend for the FlowDeps project.

For full documentation — project overview, features, architecture, and setup instructions — see the [root README](../README.md).

## Quick start

```bash
npm install
npm run dev
```

App runs at `http://localhost:3000`.
